# Web build-time env — PROD. Only VITE_* is exposed to client code. No secrets
# here (build-time public values only). Leave VITE_API_BASE_URL empty — the Caddy
# edge serves the API same-origin under /api, so no absolute URL is baked in.
VITE_API_BASE_URL=
